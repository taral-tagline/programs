"""
The program must take the directory name then walk into the full path and find all directories and all files without using an inbuilt python module(glob).
"""

import os

data = os.listdir('/Users/tagline/desktop/project/demo')
print(data)
